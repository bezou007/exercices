{% extends 'base.html.twig'%}

{% block title %}Nouvelles annonces{% endblock %}


    {% block body %}
     <div class="container">
        <h1 >Creer une nouvelle annonces</h1>
        {#{{form(form)}}  fnaovan az miaraka#}
       {{form_start(form)}}

        {{form_widget(form)}}
 
        <button type="submit" class="btn btn-primary">Creer un nouvel annonces</button>
        {{form_end(form)}} 
     </div>

    {% endblock %}